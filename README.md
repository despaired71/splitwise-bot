# Splitwise Bot для Telegram 💰

Telegram бот для учета расходов в компании с поддержкой семей и минимизацией переводов.

## ✨ Возможности

- 📝 **Мероприятия**: Создание и управление событиями (походы, встречи, поездки)
- 👥 **Участники**: Добавление участников из чата, по @username или просто по имени
- 👨‍👩‍👧‍👦 **Семьи**: Объединение участников в семьи с общим казначеем
- 💰 **Расходы**: Учет расходов с гибким распределением (поровну, на конкретных людей, свои доли)
- 🧮 **Умный расчет**: Автоматический расчет долгов с минимизацией переводов
- 📊 **Статистика**: История всех расходов и операций
- 🔒 **Права доступа**: Управление правами участников
- 📨 **Уведомления**: Автоматические уведомления о долгах

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Шаг 1: Клонирование репозитория

```bash
git clone <repository_url>
cd splitwise-bot
```

### Шаг 2: Настройка окружения

1. Создайте .env файл:
```bash
cp .env.example .env
```

2. Откройте .env и заполните:
```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Пароль для PostgreSQL
DB_PASSWORD=your_strong_password_here

# Остальные настройки можно оставить по умолчанию
```

### Шаг 3: Запуск

```bash
# Сборка и запуск всех сервисов
docker-compose up -d

# Или используйте Makefile
make up
```

Бот автоматически:
1. Запустит PostgreSQL и Redis
2. Применит все миграции БД
3. Запустится и начнет принимать сообщения

### Шаг 4: Проверка

```bash
# Посмотреть логи
docker-compose logs -f bot

# Или
make logs
```

Если видите сообщение `Bot started: @your_bot_name` - всё работает! 🎉

## 📖 Использование

### Основные команды

#### Мероприятия
- `/new_event` - создать новое мероприятие
- `/list_events` - показать все мероприятия
- `/event_info` - информация о мероприятии
- `/close_event` - закрыть мероприятие

#### Участники
- `/join_event` - присоединиться к мероприятию (в групповом чате)
- `/add_participant` - добавить участника
- `/list_participants` - список участников

#### Семьи
- `/create_family` - создать семью в мероприятии
- `/my_families` - мои шаблоны семей
- `/create_family_template` - создать шаблон семьи для переиспользования

#### Расходы
- `/add_expense` - добавить расход
- `/my_expenses` - мои расходы
- `/all_expenses` - все расходы мероприятия

#### Расчеты
- `/calculate` - рассчитать долги
- `/my_debts` - мои долги по всем мероприятиям

### Пример использования

1. **Создание мероприятия**:
   ```
   /new_event
   > Введите название: Поход в горы
   > Введите описание: Поход на выходные
   ✅ Мероприятие создано!
   ```

2. **Добавление участников**:
   ```
   /add_participant
   > Выберите мероприятие: Поход в горы
   > Выберите тип: Пользователь Telegram
   > Введите username: @ivan
   ✅ Участник добавлен!
   ```

3. **Создание семьи**:
   ```
   /create_family
   > Выберите мероприятие: Поход в горы
   > Введите название: Семья Ивановых
   > Выберите участников: Иван, Мария
   > Казначей: Иван
   ✅ Семья создана!
   ```

4. **Добавление расхода**:
   ```
   /add_expense
   > Выберите мероприятие: Поход в горы
   > Сумма: 3000
   > Описание: Аренда домика
   > Разделить: Поровну на всех
   ✅ Расход добавлен!
   ```

5. **Расчет долгов**:
   ```
   /calculate
   > Выберите мероприятие: Поход в горы
   
   🧮 Расчет долгов:
   
   💸 Должны:
   Мария (1000 ₽)
     → Иван: 1000 ₽
   ```

## 🏗️ Структура проекта

```
splitwise-bot/
├── bot/
│   ├── config/              # Настройки
│   │   └── settings.py
│   ├── database/            # База данных
│   │   ├── models/          # SQLAlchemy модели
│   │   ├── base.py
│   │   └── session.py
│   ├── handlers/            # Обработчики команд
│   │   ├── start.py
│   │   ├── event.py
│   │   ├── participant.py
│   │   ├── family.py
│   │   ├── expense.py
│   │   └── calculation.py
│   ├── keyboards/           # Клавиатуры
│   │   ├── inline.py
│   │   └── reply.py
│   ├── middlewares/         # Middleware
│   │   ├── database.py
│   │   └── auth.py
│   ├── services/            # Бизнес-логика
│   │   ├── event_service.py
│   │   ├── participant_service.py
│   │   ├── family_service.py
│   │   ├── expense_service.py
│   │   ├── calculation_service.py
│   │   └── notification_service.py
│   ├── states/              # FSM состояния
│   │   └── forms.py
│   ├── utils/               # Утилиты
│   │   ├── constants.py
│   │   ├── formatters.py
│   │   └── validators.py
│   └── main.py              # Точка входа
├── alembic/                 # Миграции БД
├── tests/                   # Тесты
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── Makefile
└── README.md
```

## 🔧 Разработка

### Работа с миграциями

**Создать миграцию:**
```bash
make migration name="add_new_table"
# или
docker-compose exec bot alembic revision --autogenerate -m "add_new_table"
```

**Применить миграции:**
```bash
make migrate
# или
docker-compose exec bot alembic upgrade head
```

**Откатить миграцию:**
```bash
docker-compose exec bot alembic downgrade -1
```

### Локальная разработка (без Docker)

1. **Создайте виртуальное окружение:**
```bash
python3.11 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Запустите PostgreSQL и Redis:**
```bash
# PostgreSQL
docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres:16-alpine

# Redis
docker run -d -p 6379:6379 redis:7-alpine
```

4. **Настройте .env:**
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=splitwise_bot
DB_USER=postgres
DB_PASSWORD=postgres
REDIS_HOST=localhost
```

5. **Примените миграции:**
```bash
alembic upgrade head
```

6. **Запустите бота:**
```bash
python -m bot.main
```

### Тестирование

```bash
# В Docker
make test

# Локально
pytest tests/ -v
```

## 📊 Архитектура БД

### Основные таблицы

**events** - Мероприятия
- id, name, description, creator_id, chat_id, status, currency

**participants** - Участники
- id, event_id, user_id, username, display_name, participant_type

**global_families** - Шаблоны семей
- id, creator_id, name, description

**families** - Семьи в мероприятиях
- id, event_id, global_family_id, name, family_head_id

**expenses** - Расходы
- id, event_id, payer_id, amount, description, split_type

**expense_splits** - Распределение расходов
- id, expense_id, participant_id, family_id, share_amount

**debt_settlements** - Расчеты долгов
- id, event_id, debtor_id, creditor_id, amount

**audit_log** - Журнал изменений
- id, event_id, user_id, entity_type, entity_id, action

## 🛠️ Полезные команды

```bash
# Запуск
make up                    # Запустить все сервисы
make down                  # Остановить все сервисы
make restart               # Перезапустить сервисы

# Логи
make logs                  # Смотреть логи бота
docker-compose logs postgres  # Логи PostgreSQL
docker-compose logs redis     # Логи Redis

# Управление
make shell                 # Зайти в контейнер бота
make clean                 # Удалить все контейнеры и данные

# База данных
make migration name="..."  # Создать миграцию
make migrate               # Применить миграции

# Мониторинг
docker-compose ps          # Статус сервисов
docker stats               # Использование ресурсов
```

## 🔍 Отладка

### Проблемы с запуском

1. **Бот не запускается:**
```bash
# Проверьте логи
docker-compose logs bot

# Проверьте что PostgreSQL готов
docker-compose logs postgres | grep "ready to accept connections"
```

2. **Ошибка подключения к БД:**
```bash
# Перезапустите сервисы
docker-compose restart

# Проверьте переменные окружения
docker-compose exec bot env | grep DB_
```

3. **Миграции не применяются:**
```bash
# Проверьте текущую версию
docker-compose exec bot alembic current

# Посмотрите историю
docker-compose exec bot alembic history
```

### Просмотр данных

```bash
# Подключитесь к PostgreSQL
docker-compose exec postgres psql -U postgres -d splitwise_bot

# Полезные SQL команды:
SELECT * FROM events;
SELECT * FROM participants WHERE event_id = 1;
SELECT * FROM expenses WHERE event_id = 1;
```

## 📝 Настройки

Все настройки в `.env` файле:

```env
# Обязательные
BOT_TOKEN=              # Токен от BotFather
DB_PASSWORD=            # Пароль БД

# Опциональные
DEBUG=False             # Режим отладки
LOG_LEVEL=INFO          # Уровень логирования (DEBUG, INFO, WARNING, ERROR)
DB_HOST=postgres        # Хост БД
DB_PORT=5432            # Порт БД
DB_NAME=splitwise_bot   # Имя БД
REDIS_HOST=redis        # Хост Redis
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел [Отладка](#-отладка)
2. Посмотрите логи: `make logs`
3. Создайте Issue в репозитории

## 🎯 Roadmap

- [x] Базовый функционал (мероприятия, участники, расходы)
- [x] Семьи и группировка участников
- [x] Алгоритм минимизации переводов
- [x] Уведомления участникам
- [ ] Поддержка нескольких валют
- [ ] Экспорт данных в CSV/Excel
- [ ] Графики и визуализация
- [ ] Recurring расходы
- [ ] Интеграция с платежными системами
- [ ] Web-интерфейс для просмотра статистики

---

Сделано с ❤️ для удобного учета расходов в компаниях
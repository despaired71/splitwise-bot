# 🚀 Быстрый старт - Пошаговая инструкция

Эта инструкция проведет вас через весь процесс от нуля до запущенного бота.

## Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `My Splitwise Bot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `my_splitwise_bot`)
5. **Сохраните токен**, который выдаст BotFather. Выглядит примерно так:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

## Шаг 2: Подготовка сервера

На вашем VPS должен быть установлен Docker и Docker Compose.

### Проверка Docker:
```bash
docker --version
docker-compose --version
```

Если не установлено, установите:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose

# Добавить текущего пользователя в группу docker
sudo usermod -aG docker $USER
# Перелогиньтесь после этого
```

## Шаг 3: Клонирование и настройка

```bash
# Перейдите в директорию для проектов
cd ~

# Клонируйте репозиторий (или загрузите zip)
git clone <your_repository_url> splitwise-bot
cd splitwise-bot

# Создайте .env файл
cp .env.example .env
```

## Шаг 4: Настройка .env

Откройте `.env` файл в редакторе:

```bash
nano .env
# или
vim .env
```

Измените следующие строки:

```env
# ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ ЭТИ ЗНАЧЕНИЯ!
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
DB_PASSWORD=придумайте_сложный_пароль

# Остальные можно оставить как есть
DB_HOST=postgres
DB_PORT=5432
DB_NAME=splitwise_bot
DB_USER=postgres
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
DEBUG=False
LOG_LEVEL=INFO
```

Сохраните файл (в nano: Ctrl+X, Y, Enter)

## Шаг 5: Запуск

```bash
# Сборка и запуск
docker-compose up -d

# Это займет несколько минут при первом запуске
```

## Шаг 6: Проверка

Проверьте что все сервисы запустились:

```bash
docker-compose ps
```

Должно показать 3 сервиса в статусе "Up":
```
NAME                 STATUS
splitwise_bot        Up
splitwise_postgres   Up (healthy)
splitwise_redis      Up (healthy)
```

Посмотрите логи бота:

```bash
docker-compose logs -f bot
```

Вы должны увидеть:
```
Bot starting up...
Database session manager initialized
Bot commands set
Bot started: @your_bot_name
Starting bot polling...
```

Если видите это - **всё работает!** ✅

Нажмите `Ctrl+C` чтобы выйти из логов.

## Шаг 7: Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Должно появиться приветственное сообщение с кнопками

### Первый тест:

```
/start
> Увидите приветствие

/new_event
> Введите название: Тестовое мероприятие
> Пропустите описание
> ✅ Мероприятие создано!

/add_expense
> Выберите мероприятие
> Введите сумму: 100
> Введите описание: Тест
> Пропустите категорию
> Выберите "Поровну на всех"
> ✅ Расход добавлен!

/calculate
> Выберите мероприятие
> Увидите расчет
```

Если все команды работают - **бот полностью готов!** 🎉

## Шаг 8: Использование в группе

1. Создайте группу в Telegram
2. Добавьте бота в группу (Add members → найдите бота)
3. Дайте боту права администратора (необязательно, но рекомендуется)
4. В группе отправьте:
   ```
   /new_event
   > Создайте мероприятие
   
   /join_event
   > Присоединитесь к мероприятию
   ```

Теперь все участники группы могут использовать бота!

## Проблемы и решения

### Бот не отвечает

```bash
# Проверьте статус
docker-compose ps

# Посмотрите логи
docker-compose logs bot

# Перезапустите
docker-compose restart bot
```

### Ошибка "database doesn't exist"

```bash
# Пересоздайте контейнеры
docker-compose down
docker-compose up -d
```

### Ошибка "token is invalid"

- Проверьте что скопировали токен правильно
- Убедитесь что нет лишних пробелов
- Проверьте файл `.env`

### База данных не подключается

```bash
# Проверьте что PostgreSQL запущен
docker-compose logs postgres

# Должно быть: "database system is ready to accept connections"
```

## Команды для управления

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Логи
docker-compose logs -f bot

# Логи всех сервисов
docker-compose logs -f

# Остановить и удалить данные
docker-compose down -v
```

## Обновление бота

```bash
# Остановить
docker-compose down

# Обновить код (если из git)
git pull

# Или загрузить новые файлы

# Пересобрать
docker-compose build

# Запустить
docker-compose up -d
```

## Автозапуск после перезагрузки

Добавьте в cron:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (замените /path/to на ваш путь)
@reboot cd /path/to/splitwise-bot && docker-compose up -d
```

Или создайте systemd service:

```bash
sudo nano /etc/systemd/system/splitwise-bot.service
```

```ini
[Unit]
Description=Splitwise Telegram Bot
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/path/to/splitwise-bot
ExecStart=/usr/bin/docker-compose up -d
ExecStop=/usr/bin/docker-compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable splitwise-bot
sudo systemctl start splitwise-bot
```

## Мониторинг

### Использование ресурсов
```bash
docker stats
```

### Размер данных
```bash
docker system df
```

### Логи с временными метками
```bash
docker-compose logs --timestamps bot
```

## Резервное копирование

### База данных
```bash
# Создать бэкап
docker-compose exec postgres pg_dump -U postgres splitwise_bot > backup.sql

# Восстановить
docker-compose exec -T postgres psql -U postgres splitwise_bot < backup.sql
```

### Все данные
```bash
# Бэкап volumes
docker run --rm \
  -v splitwise-bot_postgres_data:/data \
  -v $(pwd):/backup \
  alpine tar czf /backup/postgres_backup.tar.gz -C /data .
```

## Следующие шаги

Теперь когда бот работает:

1. Прочитайте полную документацию в [README.md](README.md)
2. Изучите все команды: `/help`
3. Создайте тестовое мероприятие и попробуйте все функции
4. Добавьте бота в ваши групповые чаты
5. Настройте регулярные бэкапы

---

Если остались вопросы - создайте Issue в репозитории!